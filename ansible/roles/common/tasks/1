---

- name: Create admins group
  ansible.builtin.group:
    name: admins
    state: present

- name: Ensure admin users exist
  ansible.builtin.user:
    name: "{{ item.name }}"
    groups: admins
    append: true
    shell: "{{ item.shell }}"
    state: present
  loop: "{{ admin_users }}"
